let text = 'hello';
let name = "헬로우 자바스크립트";

// 문자열을 선언할 때는 작은따옴표, 큰따옴표 모두 사용할 수 있다.
// 세미콜론은 있어도 되고, 없어도 된다.

let good = true;                // boolean
let loading = false;

// Null, Undefined 모두 없다를 의미하나 역할이 다르다.

let goods = null;               // 없음
let something = undefined;      // 아직 정해지지 않음

